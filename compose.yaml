services:
  database:
    image: postgres:17
    restart: always
    volumes:
      - db_data:/var/lib/postgresql/data
    env_file:
      - .env
    networks:
      - database_network
  app:
    build:
      context: .
      dockerfile: ops/Dockerfile
    ports:
      - 8000:8000
    volumes:
      - ./:/app/
    env_file:
      - .env
    depends_on:
      - database
    networks:
      - database_network

  adminer:
    image: adminer
    restart: always
    networks:
      - database_network
    depends_on:
      - database
    
networks:
  database_network:
    driver: bridge

volumes:
  db_data:
        
